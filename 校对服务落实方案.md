# 资料库网页校对服务落实方案

> 状态：根据本轮讨论整理的实施基线，尚未实施或部署。供应商许可、VPS 配置、性能与具体模型需在实施前验证。本文不授权自动修改校对技能、脚本或生产配置。

## 一、目标与边界

在现有资料库 `https://cfzx.xiyuan.wiki` 增加校对工作台，供同事登录使用。以 `proofreading` 项目中已积累的校对技能为判断基础，保留关键词扫描、四遍通读、必要的联网事实核查，不另造一套简化提示词代替原技能。

交付能力：

1. 原文精确高亮，与错误原因、修改建议、依据联动显示，减少编辑重新寻找原文的时间。
2. 独立用户账号、有限的平台试用额度、模型 BYOK，以及隔离的稿件和聊天记录。
3. 在公共宣传口径、典型错误、实战经验之上，支持个人错题本。
4. Cloudflare 承载网页及认证入口，VPS 运行 Pi 校对后台。
5. 网站统一提供搜索能力，不要求同事逐人申请搜索 API Key。

边界：

- 是专业校对服务，不是对外开放的通用 Pi 终端、Shell 或编程 Agent。
- 只报告有依据的明确错误，不混入风格润色、可选优化。
- 不自动改写原稿，不自动将模型判断沉淀为公共规则。
- 不承诺模型能够发现所有错误；最终发稿仍由编辑负责。
- 不迁移现有资料库档案，不改动现有检索和排班业务。

## 二、现有基础与复用方式

### 2.1 校对技能

技能源项目：`/home/blade/Projects/proofreading`。

主要文件：

- `.pi/skills/proofreading/SKILL.md`
- `.pi/skills/proofreading/references/authoritative/典型错误案例.md`
- `.pi/skills/proofreading/references/authoritative/气象新闻宣传口径.md`
- `.pi/skills/proofreading/references/校对实战经验.md`
- `.pi/skills/proofreading/scripts/scan-keywords.sh`

这些路径是开发 PC 上的位置，不代表 VPS 已有对应目录。

复用原则：

- `proofreading` 项目继续作为技能维护源，不在网站项目中维护另一份独立演化的规则。
- 部署使用经过确认的版本化技能包，记录版本和内容哈希；一次任务锁定同一版本，不能执行到一半混入更新后的规则。
- 每个新校对会话加载该版本的完整权威文件和参考文件，不用记忆、摘要或向量检索替代完整读取。
- 同稿追问按技能要求处理文件变更；若改用新版规则，应创建新的校对运行记录。
- 不将开发者个人 Pi 的配置目录、其他项目、全部扩展、个人记忆或凭据复制到服务端。

### 2.2 现有网站

- Worker：`china-meteo-rag`。
- 网站代码：`cmnrag-website/`。
- 当前已有注册、审批、登录与用户会话，可复用身份体系。
- 新页面建议：`/proofreading/`。
- 新业务 API 建议：`/api/proofreading/*`，默认登录后才能访问。
- 现有 D1 继续负责用户身份；校对业务不写入稿件档案表。

### 2.3 Pi 集成

采用 Pi SDK 在 VPS 的 Node.js 后台创建受控会话，不通过 iframe 嵌入终端，也不把开发者个人 Pi 会话直接暴露给同事。

SDK 的默认资源发现、凭据发现和工具加载不能直接用于多人生产环境。实施时显式配置资源、模型、凭据、会话存储和工具白名单，并固定依赖版本。

## 三、部署架构

```text
浏览器：资料库校对工作台
    │ 登录、提交稿件、查看结果、追问
    ▼
Cloudflare Worker
    ├─ 复用现有登录及账号状态校验
    ├─ 校对权限、请求限流、入口校验
    └─ 转发至受保护的校对后台
              │
              ▼
VPS：校对服务
    ├─ 任务队列、并发控制、用量账本
    ├─ Pi SDK 执行池
    ├─ 只读版本化公共规则、固定扫描工具
    ├─ 用户个人规则与独立凭据上下文
    ├─ 模型 API、搜索和正文抓取
    └─ 校对数据库、加密备份
```

### 3.1 服务边界

Cloudflare：网页静态资源、身份认证、入口访问控制、必要的转发与流式输出。

VPS：持久任务状态、校对执行、搜索调度、稿件版本、结果、追问、个人错题本、密钥使用及预算控制。

后台建议作为模块化单体服务起步，不先拆分微服务。小规模单机可评估 SQLite；需要多执行节点或更复杂并发时再考虑 PostgreSQL。选型前确认备份、事务和恢复要求。

### 3.2 Cloudflare 与 VPS 之间的信任

- 使用 TLS 及受保护的服务连接；可评估 Cloudflare Tunnel，具体方案上线前确定。
- VPS 不接受浏览器自行声明的 `user_id`，也不能信任任意客户端发送的用户标头。
- Worker 验证身份后使用短期签名凭证等机制传递用户身份，后台校验签名、有效期与受众；密钥不得出现在浏览器。
- 后台仍按用户归属校验每个稿件、任务、历史、导出和流式订阅请求。
- 禁用账户后应阻止新操作并使访问失效；复用现有认证时要审计账号状态和会话撤销行为，不能默认已有实现完全满足新业务。

### 3.3 为什么优先混合部署

- 网站和登录已经运行于 Cloudflare，无需迁走。
- Pi SDK、现有 Shell 扫描和任务管理更容易在 Linux VPS 上直接复用。
- 不在 VPS 本地运行大模型，因此无需 GPU。
- 普通 Worker 不适合不加适配地运行当前 Node/Pi 加 Shell 工作流。
- Cloudflare Sandbox/Containers 具备 Linux 执行能力，是后续替代选项，不是技术上不可行；首期不引入其额外的容器、持久化和计费设计。

## 四、校对执行流程

### 4.1 单任务工作流

1. 校验用户权限、额度、稿件长度和并发限制。
2. 接收稿件及稿件日期、拟刊日期，保存不可变版本和段落标识。
3. 固定本次技能版本、个人错题本版本、模型及搜索策略。
4. 加载完整权威文件和参考文件；个人规则仅作为补充。
5. 服务端在本任务独立工作目录写稿，调用固定扫描脚本，保存扫描线索。
6. 四遍全文通读：政治与事实 → 文法与标点 → 口径固定搭配 → 机构全称与准确性。
7. 按经用户确认的技能执行顺序完成必要的联网核查。
8. 合并重复问题，验证证据、原文位置和建议结构，按原文出现顺序输出。
9. 保存结果、证据及用量，释放会话资源，只清理本任务临时文件。

扫描是线索输入，不代替通读；扫描未命中不等于合规。第三遍仍须遍历宣传口径各类条目，标题、副标题、图注、作者行不得豁免。

### 4.2 流程可验证性

- 保存规则加载、扫描、四遍检查、联网核查、定位验证等阶段的完成记录。
- 记录可供审核的覆盖情况、来源及结论，不要求保存或展示模型完整内部推理。
- 页面进度必须来自实际执行事件，不能使用模拟进度冒充已完成检查。
- 不能只因模型声称“四遍已完成”就认为流程可靠；需要任务编排、产物校验和历史案例回归共同验证。
- 分阶段调用与单会话编排的具体方式由原型对照测试决定，不未经验证就改动既有技能效果。

### 4.3 上线前需确认的技能适配

现有文件中存在以下需要明确的地方，实施时先提出变更方案，经用户确认后修改：

1. 无错输出已按用户确认统一：`AGENTS.md` 与 `SKILL.md` 均规定全文无错时输出“无意见”。网页沿用“无意见”，并通过任务状态与未完成、失败情况区分。
2. 统一联网查证的执行时机；保留默认本地判断和必要联网的边界，不擅自扩展为无限背景调查。
3. 当前草稿清理使用 `rm drafts/*.md`，不能用于多人共享目录。改为任务隔离，仅删除本任务已知临时文件。
4. 保留每篇一次合并搜索、必要时一次追加抓取的现有约束；如增设深度核查模式，单独定义规则、耗时和费用。
5. 网页结果需要结构化输出，但不得借此改变“只报明确错误、按原文顺序排列”的判断原则。

### 4.4 状态与故障处理

任务至少区分：排队、执行中、完成、部分核查未完成、失败、取消。

- 任务运行不依赖浏览器连接，刷新或断网后可凭任务 ID 恢复查看。
- 提交支持幂等标识，避免重复点击、网络重试造成重复任务和付费。
- 不承诺中断的模型调用能够原地恢复；重试需记录新尝试并控制重复费用。
- 取消时终止可取消的调用并清理资源，但不能保证供应商已执行的请求不收费。
- 搜索失败、证据不足或模型中断不得转化成“无意见”。

## 五、网页与结果交互

### 5.1 页面布局

- 左侧原文，右侧错误卡片；追问放在底部或侧栏。
- 点击卡片自动定位对应原文，点击高亮切换到对应卡片。
- 标签使用“政治、文法、口径、准确”，不只依赖颜色区分。
- 提供采纳、忽略、提出异议、加入个人错题本操作。
- 原稿不覆盖；采纳后创建修订稿及操作记录。
- 不默认一键接受所有建议，涉及事实、姓名、职务和口径的修改需要人工确认。

错误卡片字段：

- 原文片段及具体高亮范围；
- 错误类别、原因；
- 修改建议，无法确定替换文本时允许提示需补充信息；
- 依据关键词或外部证据链接；
- 当前处置状态。

“待核实”单列，不混入明确错误列表。引用公共规则以内容关键词定位，不依赖易变的条目序号。

### 5.2 原文定位

每条问题绑定稿件版本和段落 ID，模型返回原文片段及必要前后文，由程序验证并解析字符位置。不能直接信任模型生成的偏移量。

需覆盖：同一片段多次出现、换行与空格变化、中英文混排、重叠问题和跨段问题。不能唯一匹配时提示定位失败，不高亮猜测位置。

原文编辑后创建新版本；旧建议不能直接应用到新版本。应用修改前校验目标片段仍匹配；多个修改存在重叠或偏移变化时重新计算，禁止盲目批量替换。

## 六、用户、BYOK 与额度

### 6.1 用户体系

- 每位同事独立账号，不共用测试账号。
- 复用注册审批；每个获准用户拥有有限平台试用额度。
- 账号身份与额度分离，额度耗尽不删除历史记录。
- 管理员默认查看运行和用量指标，是否允许查看稿件正文需另定隐私政策及审计机制。

### 6.2 模型 BYOK

首期只接入少量经过回归验证的提供商和模型。配置包括提供商、模型、Key、连通性与工具能力检查。任务显示实际模型和付费来源。

- 模型 Key 与搜索 Key 分开；用户仅需自带模型 Key。
- 用户 Key 失败不能静默切换成平台付费。
- 不接受任意第三方 API 地址；确有需要时经审核进入白名单，防止 SSRF 和凭据外泄。
- 不复用个人订阅凭据给多人提供服务，除非相应供应商明确许可。
- 提供商不同可能影响校对质量，不保证任何兼容 API 都达到同样效果。

凭据策略：

- 提供“仅本次使用、不持久保存”选项；此模式应说明服务重启后可能需要重新提供 Key。
- 持久保存采用服务端加密，主密钥与数据库分离，支持更换、删除与轮换。
- 不写入聊天、提示词、普通日志、错误堆栈或浏览器 `localStorage`。
- 不通过修改全局环境变量切换用户 Key；每任务独立凭据上下文。
- 后台代理调用仍会在使用时接触 Key，不能宣传为服务器完全不可见。

### 6.3 预算与账本

同时控制单篇长度、模型输出量、任务时长、搜索次数、单用户并发、日预算及平台总预算。

用量账本至少记录任务、用户、提供商、模型、输入输出用量、搜索请求量、重试、付费来源、预估与实际可得成本。

平台额度采用原子预留与结算，防止并发任务超支；取消和失败也可能产生费用。设置总预算熔断，不只依靠前端计数。

## 七、搜索服务方案

### 7.1 已讨论的选型结论

用户个人使用 Tavily 每月 1,000 次已接近上限，因此不再把 Tavily 免费层作为多人主搜索容量。

首期优先：**网站专用 TinyFish Search/Fetch 为主，项目 Exa API 免费额度为备用；免 Key Exa MCP 只作为可评估的辅助路径。**

网页用户不需要自行申请搜索账号。后台保管项目凭据，与个人账号使用和费用隔离。

### 7.2 本轮核查到的供应商条件

以下是讨论期间查到的公开信息，不是长期承诺，实施和上线时必须重新核对。

| 服务 | 本轮核查结果 | 设计含义 |
|---|---|---|
| TinyFish Search | 免费；30 请求/分钟、500 请求/小时；价格页未列月度上限 | 优先作为主搜索，不等于无限并发或永久无限免费 |
| TinyFish Fetch | 免费；150 URL/分钟、1,000 URL/天 | 需要按 URL 数量统计，独立管理日额度 |
| TinyFish Agent / Browser | 付费 | 不自动调用，不作为免费线路的隐式兜底 |
| Exa 免 Key MCP | 官方提供免费但受限流的模式；本轮未查到明确容量承诺 | 可试验，不据此规划生产容量 |
| Exa 有 Key API | 每月 10 美元免费 credits；普通 Search 基础档每千次 7 美元 | 仅用于该档搜索时约 1,428 次/月，适合备用而非主容量 |
| Tavily | 每月 1,000 credits；基础搜索 1 credit/次，高级搜索 2 credits/次 | 可保留为其他备选，不承担主线 |

Exa 价格会受到搜索类型、结果数量及附加能力影响，不能把估算次数视为固定赠送次数。

### 7.3 TinyFish 账号和许可确认

可以考虑项目专用账号或独立 Key，但先确认供应商支持的项目隔离方式。

TinyFish 条款包含个人或内部业务用途以及转售、转授限制。上线前向官方确认：供本单位同事登录使用的内部稿件校对工具，后台统一调用免费 Search/Fetch、不暴露 Key、不转售搜索接口，是否被允许；同时确认项目账号与个人账号分离的方式。

不得采用多账号轮换绕过供应商配额。若未来面向单位外部或收费开放，需要重新审查许可。

### 7.4 pi-web-access 接入边界

本轮检查到的扩展行为：

- Exa 未配置 Key 时走免 Key MCP；配置 Key 时走直接 API，使用账号额度。
- 自动路由可能使用可发现的其他凭据和供应商。

网站必须使用独立配置和供应商白名单，不能照搬个人默认自动兜底链，避免调用开发者个人 Codex、浏览器 Cookie 或其他付费凭据。

明确指定主备顺序、允许兜底的故障类型和预算。限流先排队并按供应商要求退避；确认不可用后才切备用，禁止无限重试或并行向所有供应商发请求。

### 7.5 查询、抓取与缓存

- 单次工具调用包含多条 `queries` 时，按实际外部请求统计，不能当成一次 API 消耗。
- 限速覆盖全站，而不是每用户分别按供应商上限运行。
- 已有官方链接时优先读取原文；普通直接抓取不消耗搜索 API 次数，但有服务器开销和网站限制。
- 用户输入链接和抓取重定向须执行 SSRF 检查，不允许读取内网、元数据地址或本地文件。
- 事实判断保留官方 URL、相关原文片段、抓取时间及适用日期，不能只依赖搜索摘要。
- 官方来源优先，但搜索未命中不代表事实不存在或稿件无错。
- 跨用户缓存只放可安全复用的公开证据，遵守来源条款；未刊稿片段和个人查询上下文不能进入公共缓存。
- 职务、现行政策、实时事件设置适当有效期；历史稿按稿件日期判断。
- 向搜索供应商仅发送必要事实点，不发送整篇未刊稿或内部口径全文。

### 7.6 免费服务耗尽或不可用

本地扫描和通读可继续，但需要联网才能判定的内容标为未完成。允许用户补充官方链接，不得将未核实事项报为已通过。

不承诺永久零费用且随时可用。是否启用小额付费兜底由用户另行确认，默认不自动充值或扩大预算。

## 八、个人错题本与公共规则

层级：

1. 公共权威口径、典型错误：由用户维护，是统一判断依据。
2. 公共实战经验：由用户审核后维护。
3. 个人错题本：各账号私有，补充个人易错点、机构专名及适用场景。

个人条目建议包含错误写法、正确写法、原因、来源、适用范围、例外、有效期和启用状态。风格偏好单列，不作为明确错误依据。

- 个人规则不能覆盖公共权威规则，冲突必须提示。
- “加入错题本”生成待确认草稿，不直接自动保存模型结论。
- 用户接受一次修改不等于该规则具有普遍适用性。
- 提供“申请加入公共经验”，由维护者审核，不自动改变共享文件或扫描脚本。
- 规则作为受控数据输入，不允许借个人错题本改变系统权限、执行任意工具或加载外部配置。
- 限制个人规则数量和长度；规模变大后再评估检索方案，不能导致公共口径被挤出上下文。

## 九、数据模型与保存政策

建议关系：

```text
用户
 ├─ 模型凭据引用、额度与账本
 ├─ 个人规则及版本
 └─ 稿件
     ├─ 稿件版本
     └─ 校对任务
         ├─ 执行尝试、阶段事件
         ├─ 模型与规则版本快照
         ├─ 问题项与原文锚点
         ├─ 证据来源
         ├─ 追问消息
         └─ 修改处置与修订稿
```

每篇新稿建立独立上下文；不要把全部稿件串入一条无限增长的聊天。业务记录与 Pi 会话存储的映射须明确，不能同时维护两份互相不一致的权威历史。

上线前确定：

- 原稿、修订稿、聊天、证据及附件的保留时长；
- 用户导出和删除范围；
- 日志脱敏及保存周期；
- 删除数据在备份中的到期方式；
- 管理员访问正文的边界；
- 备份加密、恢复演练和服务故障恢复目标。

备份与批量删除遵循项目纪律：删除前备份，批量操作先 dry-run 确认。明确告知备份副本不会因在线删除立刻消失，按既定周期到期。

## 十、资源预算与运行方式

### 10.1 资源判断

模型推理在外部 API，VPS 不需 GPU，也不按用户数加载本地模型。资源主要消耗于 Node/Pi 会话上下文、网页内容解析、脚本执行、数据库及网络连接。

本轮讨论的预算仅用于规划，不是实测性能或容量保证：

| 阶段 | 为校对服务预留的资源 | 初始执行并发 |
|---|---|---|
| 小范围验证 | 约 1 vCPU、1GB 内存 | 1 个任务 |
| 日常试运行 | 约 2 vCPU、2GB 内存 | 2 个任务 |
| 留出更大余量 | 约 2–4 vCPU、4GB 内存 | 实测后决定 |

若现有 VPS 为 2 核 4GB 且其他负载较低，可优先复用，但需先核实配置。上述是服务可用预算，不意味着整机已有业务可无限共享。

### 10.2 执行池

- 少量执行进程按需创建任务会话，不为每位用户常驻一个 Pi 进程。
- 执行完成保存记录并释放内存；历史存储不等于会话常驻。
- 起步采用受控执行池、独立任务目录和只读规则包；进程/容器隔离方式按威胁模型确认。
- 工具白名单不是完整沙箱，仍需操作系统权限、文件路径边界和网络访问限制。
- 不开放通用 Bash，仅执行受控固定脚本，参数使用安全传递方式。
- 首期不本地运行 Chromium、OCR 或复杂附件转换，避免不可控资源峰值。

### 10.3 压测

用短稿、长稿、联网核查较多的真实案例，测试 1、2、4 个并发任务，记录进程组内存峰值、CPU、耗时、完成后内存回落、模型和搜索限流。

依据实测确定正式并发、单篇上限和排队策略，并为系统与其他服务留余量。候选瓶颈包括供应商限速、费用、上下文长度和 VPS 内存，不预先认定单一瓶颈。

## 十一、安全与保密上线门槛

1. **外部传输许可**：确认未刊稿、内部宣传口径、经验文件能否发送给所选模型；BYOK 不是保密豁免，自建 VPS 也不代表数据不外传。
2. **模型供应商白名单**：既考虑能力，也考虑内部资料授权与数据使用政策。
3. **最小权限**：不挂载个人配置、项目根目录 `.env`、其他项目文件或无关凭据；只暴露所需规则和当前任务数据。
4. **跨用户隔离**：覆盖读取、修改、任务订阅、导出、缓存、临时文件、凭据和错误日志。
5. **输入安全**：稿件、个人错题本和网页均视为不可信数据，不能借提示注入获得执行权限。
6. **页面安全**：错误卡片、抓取正文、模型 Markdown 和用户内容均做安全渲染，防止 XSS；变更接口做 CSRF/Origin 等必要防护。
7. **滥用限制**：登录限流、任务限流、长度限制、并发限制和全站预算熔断。
8. **密钥生命周期**：加密、脱敏、轮换、撤销；网站专用搜索凭据与个人凭据分离。
9. **故障语义**：不把超时、额度耗尽、证据不足包装成检查通过。
10. **依赖与发布**：固定 Pi 和扩展版本，升级先回归，不自动跟随最新版上线。

## 十二、实施阶段与验收

### 阶段 0：确认运行与合规条件

待完成：

- 核查 VPS 配置、现有业务负载、地域、磁盘、备份及网络可达性。
- 确认 TinyFish 内部多人使用许可及项目账号方式。
- 确认首批模型、平台试用预算和允许外传的资料范围。
- 确认前述技能适配、保存期限与管理员正文访问政策。
- 建立去敏后的历史回归样本。

产物：确认清单、技能适配变更提案、供应商及部署参数。未确认的条款和保密问题不得以技术可用替代。

### 阶段 1：校对引擎与定位原型

实现：

- VPS Pi SDK 原型、受控工具与技能版本加载。
- 固定扫描和四遍校对流程、TinyFish 联网核查。
- 结构化错误项、证据和原文锚点验证。
- 单任务执行及真实阶段事件。

验收：

- 与当前个人校对方式对照，不丢失规则覆盖。
- 历史明确错误能检出，已核销合规表达不重新误报。
- 扫描命中经上下文复核，未命中不跳过通读。
- 无证据不输出确定性错误；无法核查不显示通过。
- 同片段多次出现及版本变化不产生错误高亮或误替换。

### 阶段 2：工作台与小范围内测

实现：

- `/proofreading/` 原文与卡片联动、追问、采纳及修订稿。
- 复用登录审批，后台鉴权与用户归属校验。
- 独立账号、有限平台额度、任务队列与持久化。
- 刷新恢复、取消、重试及防重复提交。

验收：

- 用户无法读取或操作其他用户的任何稿件和任务。
- 断网刷新不重复付费提交，任务可恢复查看。
- 被禁用用户不可继续使用已有会话绕过限制。
- 不影响既有资料库和排班服务。

### 阶段 3：BYOK、个人错题本与正式运行准备

实现：

- 模型凭据加密、按任务隔离、用量预留与结算。
- 个人错题本确认流程、冲突提示和公共经验申请。
- Exa 备用、搜索统一限速和公开证据缓存。
- 备份恢复、数据保留和管理员审计。

验收：

- 用户凭据不串用、不泄露，不静默消耗平台余额。
- 并发任务不突破预算控制，不误删他人草稿。
- 主搜索限流或不可用时可排队/受控切换，备用耗尽后正确降级。
- 个人规则不覆盖公共规则，不自动污染公共经验。
- 完成 1/2/4 并发测试与恢复演练，确定生产限额。

### 阶段 4：后续增强，不纳入首期承诺

Word 导入导出及修订模式、复杂 PDF/OCR、更多模型、深度事实核查、公共经验审核界面、多节点执行。每项先评估保密、定位、资源与费用影响。

## 十三、质量评估与运维指标

回归集覆盖政治表述、固定口径、文法、机构专名、日期与职务、作者行、标题图注、重复片段、长稿、联网不足及历史误报核销案例。

记录并定期复核：

- 明确错误检出情况与漏报；
- 合规内容误报；
- 原文定位和修改应用准确性；
- 官方来源命中率及证据是否真正支持结论；
- 各阶段耗时、排队时间、失败与重试率；
- 每篇模型和搜索用量、平台费用；
- 进程资源峰值、备份状态与恢复结果。

具体质量阈值由用户基于历史样本确认，不预设未经测量的准确率或吞吐量。更新模型、规则、脚本、Pi 或搜索扩展均须回归；生产发布保留可回滚版本。

## 十四、首期结论

- **产品形态**：专业校对工作台，原文定位为核心，聊天为辅助。
- **部署**：Cloudflare 网站与认证入口 + VPS Pi SDK 后台。
- **技能**：复用 `proofreading` 权威源，经确认后做多人安全和结构化输出适配。
- **用户**：独立账号 + 平台有限试用额度 + 模型 BYOK，不共享测试账号。
- **搜索**：项目 TinyFish 为主，Exa 有 Key 免费额度为备用；免 Key MCP 不承担容量承诺。
- **规则**：公共规则由维护者审核，个人错题本隔离且不能覆盖权威规则。
- **资源**：先按为服务预留约 2 vCPU、2GB 内存、2 个执行并发规划，最终以实测为准。
- **交付顺序**：先证明校对效果与定位正确，再补齐多人能力，最后扩展附件和高级功能。

## 十五、参考资料

实施前重查当前版本和条款：

- TinyFish 定价：https://www.tinyfish.ai/pricing
- TinyFish 服务条款：https://www.tinyfish.ai/terms
- Exa MCP：https://docs.exa.ai/reference/exa-mcp
- Exa 定价：https://exa.ai/pricing
- Tavily credits：https://docs.tavily.com/documentation/api-credits
- Cloudflare Sandbox：https://developers.cloudflare.com/sandbox/
- Cloudflare Workers 限制：https://developers.cloudflare.com/workers/platform/limits/

本轮核查的本地资料：

- `cmnrag-website/src/auth.ts`
- `cmnrag-website/wrangler.jsonc`
- `/home/blade/Projects/proofreading/AGENTS.md`
- `/home/blade/Projects/proofreading/.pi/skills/proofreading/SKILL.md`
- `/home/blade/.local/share/mise/installs/pi/0.87.1/pi/docs/sdk.md`
- `/home/blade/.pi/agent/npm/node_modules/pi-web-access/README.md`

上述绝对路径只描述当前开发环境；实施时确认实际安装版本和 VPS 路径，不直接照抄部署。
