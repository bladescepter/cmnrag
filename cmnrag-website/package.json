{
  "name": "china-meteo-rag",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest --config vitest.unit.config.mts",
    "cf-typegen": "wrangler types",
    "build:paiban": "npm --prefix paiban-web run build",
    "deploy:paiban": "npm run build:paiban && npm run deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.115.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.9.0",
    "hono": "^4.13.3",
    "xlsx": "^0.18.5",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
